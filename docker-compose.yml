version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: autoyolo-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    networks:
      - autoyolo-network

  redis:
    image: redis:latest
    container_name: autoyolo-redis
    ports:
      - "6379:6379"
    networks:
      - autoyolo-network

networks:
  autoyolo-network:
    driver: bridge

volumes:
  mongodb_data:
